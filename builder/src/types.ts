// Re-export shared types — single source of truth is kapow-shared
export type {
  Task, Phase, ArchitectureDoc, Artifact,
  TaskBuildRequest, TaskBuildResult, TaskFixRequest,
  ShellResult, AvailableTool,
} from 'kapow-shared';
